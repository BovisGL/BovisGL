package com.bovisgl.proxy.commands

/**
 * Ban commands have been disabled - all banning is now centralized through the web interface
 * The communications service handles all ban checks
 * 
 * Removed classes:
 * - BanCommand (was /ban)
 * - UnbanCommand (was /unban)
 * 
 * These commands are no longer registered in CommandManager
 */

